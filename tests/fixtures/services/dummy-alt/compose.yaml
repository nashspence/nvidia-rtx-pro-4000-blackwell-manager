services:
  web:
    image: busybox:1.36
    command:
      - sh
      - -c
      - mkdir -p /www && printf 'alt' >/www/index.html && exec httpd -f -p 8080 -h /www
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080"]
      interval: 1s
      timeout: 2s
      retries: 15
